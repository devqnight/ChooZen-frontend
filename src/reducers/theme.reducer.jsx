const initialState = {
    bar: {

    },
    accentColor: "orchid"
}

const theme = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default theme;